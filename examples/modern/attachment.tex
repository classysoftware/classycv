\documentclass[a4paper, 12pt]{article}

\usepackage[draft]{graphicx}
\usepackage{tikz}

\begin{document}
\pagestyle{empty}
\pagecolor{gray}
\centerline{\Huge\sffamily\color{white}\fontseries{l}\selectfont\MakeUppercase{Attachment}}
\vfill
\centerline{%
    \tikz{%
        \draw[draw=white, fill=gray, line width=5pt, use as bounding box] rectangle (0.8\textwidth, 0.8\textheight);
        \draw[white, line width=5pt] ([xshift=5pt, yshift=-2.5pt]current bounding box.north west) -- ([xshift=-5pt, yshift=2.5pt]current bounding box.south east);
        \draw[white, line width=5pt] ([xshift=5pt, yshift=2.5pt]current bounding box.south west) -- ([xshift=-5pt, yshift=-2.5pt]current bounding box.north east);
        \node[scale=10, fill=gray] at (current bounding box.center) {\color{white}A};
    }
}
\vfill
\vfill
\centerline{%
    \tikz{%
        \draw[draw=white, fill=gray, line width=5pt, use as bounding box] rectangle (0.8\textwidth, 0.8\textheight);
        \draw[white, line width=5pt] ([xshift=5pt, yshift=-2.5pt]current bounding box.north west) -- ([xshift=-5pt, yshift=2.5pt]current bounding box.south east);
        \draw[white, line width=5pt] ([xshift=5pt, yshift=2.5pt]current bounding box.south west) -- ([xshift=-5pt, yshift=-2.5pt]current bounding box.north east);
        \node[scale=10, fill=gray] at (current bounding box.center) {\color{white}B};
    }
}
\vfill
\end{document}